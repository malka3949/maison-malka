# דוח תאימות — תיקון 13 לחוק הגנת הפרטיות ותקנות אבטחת מידע 2017

**תאריך:** 2026-07-22  ·  **מערכת/פרויקט:** Maison Malka  ·  **נתיב שנבדק:** `C:\Users\user1\Desktop\Masion-malka`
**הופק ע"י:** israel-privacy-compliance (ביקורת אוטומטית, read-only)

> ⚠️ **אזהרה:** מסמך זה הוא ניתוח תאימות **טכני**, ולא ייעוץ משפטי. עמידה מלאה בחוק דורשת גם תשתיות
> מאובטחות, תהליכים ארגוניים, וליווי משפטי. הממצאים מבוססים על סריקת קוד בלבד.

## סטטוס תיקון (Wave 1) — 2026-07-22

| פער | סטטוס |
|---|---|
| עיון / ייצוא PII להזמנה | **טופל** — `exportOrderPiiAction` + UI באדמין |
| התמתת PII בהזמנה | **טופל** — `anonymizeOrderPiiAction` + UI |
| הוכחת הסכמה ב-checkout | **טופל** — `terms_accepted_at` + `legal_consent_version` |
| הסכמה ברישום | **טופל** — checkbox + אכיפת שרת |
| Audit log לפעולות רגישות | **טופל חלקית** — `AccessAuditLog` (סטטוס/ייצוא/התמה) |
| MFA לאדמין | **שיורי** — להפעיל ב-Supabase (ראו `PRODUCTION-LAUNCH.md`) |
| נוסח משפטי סופי | **שיורי** — טיוטה מקצועית + `draftNotice` עד אישור עו״ד |

---

## 1. תקציר מנהלים

Maison Malka היא חנות מאפים מקוונת (guest checkout + חשבון אופציונלי) ששומרת PII של לקוחות (שם, טלפון, אימייל, כתובת משלוח) והיסטוריית הזמנות/סכומים. לפי תיקון 13, היסטוריית עסקאות נחשבת **מידע בעל רגישות מיוחדת** (פעילות פיננסית) → **רמת אבטחה בינונית** לפחות. הפער החמור ביותר: אין מנגנוני עיון/מחיקה לנושא-מידע (חשיפה לסעיף 15א) ואין MFA / audit-log למורשי-גישה למאגר.

| חומרה | כמות |
|---|---|
| 🔴 קריטי | 4 |
| 🟡 סיכון | 7 |
| 🔵 קל | 2 |

**שלושת הפערים לטיפול מיידי:**
1. 🔴 זכויות נושא-מידע — אין API/תהליך עיון מרוכז על כל המידע האישי — סעיף 13 / סיכון 15א
2. 🔴 זכויות נושא-מידע — אין hard-delete / התממה של PII בהזמנות/פרופיל לפי בקשת מחיקה — סעיף 14 / סיכון 15א
3. 🔴 בקרת גישה — אין MFA למורשי admin שצופים ב-PII — תקנה 9(ב)(1)

---

## 2. סיווג מידע ורמת אבטחה

**סוגי מידע שזוהו:**

| שדה / מקור | סיווג | היכן נשמר | רגיש-מיוחד? |
|---|---|---|---|
| `Order.customer_name` | אישי | PostgreSQL (Supabase) | לא |
| `Order.customer_phone` | אישי | PostgreSQL | לא |
| `Order.customer_email` | אישי | PostgreSQL + Resend | לא |
| `Order.delivery_address` | אישי | PostgreSQL (+ Resend לאדמין) | לא* |
| `Order.customer_notes` | אישי (עלול להכיל PII חופשי) | PostgreSQL + Resend | לא |
| `Order.subtotal` / `total` / `payment_method` + היסטוריית הזמנות | **רגיש-מיוחד** (פעילות פיננסית) | PostgreSQL | **כן** |
| `CustomerProfile` (full_name, phone, email, default_delivery_address) | אישי | PostgreSQL | לא |
| `User.email` + תפקיד | אישי | PostgreSQL + Supabase Auth | לא |
| סיסמת התחברות | סוד אימות | Supabase Auth (לא באפליקציה) | — |
| אימייל טרנזקציונלי (לקוח/אדמין) | אישי (+ פיננסי באדמין) | Resend (צד-ג') | חלקי |
| תרגום שם מוצר (Gemini, אופציונלי) | לא-אישי (קטלוג) | Google Generative Language API | לא |
| עגלת קניות (client) | לא-אישי / מזהי מוצר | דפדפן הלקוח | לא |

\* כתובת רחוב למשלוח סווגה כמידע אישי (לא GPS). אם ייעוץ משפטי יסווג כתובת כ"נתוני מיקום" לפי תיקון 13 — הרמה נשארת בינונית בכל מקרה בגלל הפעילות הפיננסית.

**רמת אבטחה נדרשת (תקנות 2017):** **בינונית**
**נימוק:** קיום **מידע בעל רגישות מיוחדת** — היסטוריית הזמנות, סכומים ואמצעי תשלום (= פעילות פיננסית לפי סעיף 3 תיקון 13). מספר מורשים צפוי קטן (<10), היקף לקוחות MVP לא מגיע ל-100,000 → לא רמה גבוהה; אך רגיש-מיוחד קובע **רצפת בינונית** (MFA, audit-log, הצפנה במעבר).

---

## 3. ניתוח פערים לפי קטגוריה

### א. סיווג מידע (סעיף 3) — ⚠️

- 🟡 `prisma/schema.prisma:165-187` — שדות הזמנה כוללים סכומים ו-`payment_method` לצד זהות הלקוח; הסיווג כרגיש-מיוחד (פעילות פיננסית) אינו מתועד במדיניות/מסמך מאגר. **סעיף/תקנה:** סעיף 3 — "מידע אישי על … פעילותו הפיננסית". **תיקון:** לתעד במסמך הגדרות-מאגר ובמדיניות הפרטיות שהמאגר כולל מידע רגיש-מיוחד ולנהל בהתאם לרמה בינונית.
- 🔵 אין איסוף בריאות/ביומטרי/אשראי בכרטיס — מפחית היקף; אין סליקה מקוונת (תואם הקשר MVP).

### ב. צמצום מידע, מטרה והסכמה (סעיפים 8, 11; תק' 2(ג)) — ⚠️

- 🟡 `src/content/legal/privacy.ts:12-43` — מדיניות פרטיות מסומנת כטיוטת MVP; חסרים במפורש חלק מחובות סעיף 11 (האם מסירה חובה/רצון, מטרות מסירה לכל נמען, תקופת שמירה, זכויות עיון/תיקון/מחיקה). **סעיף/תקנה:** סעיף 11(1)–(3). **תיקון:** להחליף טיוטה בנוסח שאושר משפטית עם כל רכיבי היידוע.
- 🟡 `src/components/storefront/CheckoutForm.tsx:238-266` + `src/lib/actions/orders.ts:182-223` — יש checkbox להסכמה לתנאים/פרטיות ונאכף בשרת (`acceptedTerms`), אך **ההסכמה אינה נשמרת** (זמן, גרסת מסמך, IP/מזהה). **סעיף/תקנה:** סעיף 11 + הוכחת הסכמה מדעת. **תיקון:** לשמור רשומת הסכמה בהזמנה (timestamp + version hash של המדיניות).
- 🟡 `src/app/[locale]/register/page.tsx:25-47` — רישום אוסף שם/טלפון/אימייל/סיסמה **בלי** הודעת פרטיות או checkbox הסכמה. **סעיף/תקנה:** סעיף 11. **תיקון:** אותה שכבת יידוע+הסכמה כמו ב-checkout.
- 🟡 אין מדיניות retention / מחיקה יזומה להזמנות ישנות — מידע נשמר ללא הגבלת זמן בקוד. **סעיף/תקנה:** תקנה 2(ג) — בחינה שנתית לצמצום. **תיקון:** להגדיר retention (למשל אחרי תקופת חשבונאות — התממת PII) ותזכורת שנתית.
- ✅ מטרת האיסוף ב-checkout (ביצוע הזמנה + יצירת קשר) מוצהרת ב-`privacyNotice` ובטיוטת המדיניות — כיוון נכון לסעיף 8(ב).
- ✅ אין סליקת כרטיס — צמצום טוב של מידע פיננסי רגיש.

### ג. בקרת גישה, הזדהות ולוגים (תקנות 8, 9, 10) — ❌

- 🔴 `src/lib/actions/auth.ts:8-35` + `src/lib/auth.ts:13-28` — התחברות admin בסיסמה בלבד (Supabase password); **אין MFA/2FA** בקוד או באכיפה. מאגר ברמה בינונית מחייב זיהוי חזק. **תקנה:** 9(ב)(1) — זיהוי על אמצעי פיזי בשליטת המורשה. **תיקון:** לאכוף MFA ב-Supabase Auth לכל משתמשי admin (AAL2) לפני גישה ל-`/admin`.
- 🔴 אין מנגנון audit-log אוטומטי לגישה למידע אישי (צפייה בהזמנות, עדכון סטטוס, ייצוא). לוגים קיימים הם `console.*` לתקלות מייל בלבד. **תקנה:** 10(א) — תיעוד: זהות, זמן, רכיב, סוג/היקף, אושר/נדחה; שמירה 24 חודשים. **תיקון:** טבלת `AccessAuditLog` + כתיבה ב-`requireAdmin` ובפעולות על `Order`/`CustomerProfile`, כולל ניסיונות שנדחו.
- 🟡 `src/app/admin/(protected)/orders/[id]/page.tsx:61-91` — כל admin עם role רואה PII מלא; אין הפרדת תפקידים (support vs owner). למאפייה קטנה סביר, אך אין least-privilege מעבר ל-binary admin/customer. **תקנה:** 8(א). **תיקון:** אם יתווספו מורשים — תפקידים מצומצמים (למשל ללא ייצוא מלא).
- ✅ `requireAdmin` נאכף ב-layout ובפעולות server — בדיקת תפקיד בצד-שרת קיימת.

### ד. הצפנה והפרדת רשתות (תקנות 12, 13, 14) — ⚠️

- 🟡 `next.config.ts:27-50` — אין HSTS / כפיית HTTPS ברמת האפליקציה (תלוי ב-Vercel בפרודקשן). **תקנה:** 14(ב) — העברה ברשת ציבורית בהצפנה מקובלת. **תיקון:** להוסיף `Strict-Transport-Security` ב-headers לפרודקשן ולוודא redirect HTTP→HTTPS.
- 🔵 קריאות outbound ל-Resend ול-Gemini משתמשות ב-`https://` עם TLS רגיל — תקין בקוד.
- *(קונטקסט תשתית, לא פער-קוד)* הצפנה במנוחה ב-Supabase Postgres / הפרדת רשתות — יש לאמת מול הגדרות הפרויקט והסכמי עיבוד; תקנה 12/13.

### ה. זכויות נושא המידע — עיון/תיקון/מחיקה (סעיפים 13, 14; סיכון 15א) — ❌

- 🔴 אין מסלול עיון/ייצוא שמאגד `CustomerProfile` + כל `Order` (+ הערות) לנושא-מידע. אדמין רואה הזמנה בודדת; הלקוח רואה אישור הזמנה חלקי בלי PII מלא, בלי ייצוא. **סעיף/תקנה:** סעיף 13(א); חשיפה לפיצויים לדוגמה — **סעיף 15א**. **תיקון:** API/כלי אדמין מאומת: "ייצוא כל המידע על אימייל/טלפון X" + תהליך מענה לבקשות.
- 🔴 אין hard-delete / anonymize ל-PII בהזמנות או מחיקת פרופיל לקוח לפי בקשה. מחיקות בקוד הן למוצרים/קטגוריות/מדיה בלבד. רשומות `Order` נשארות עם שם/טלפון/אימייל/כתובת לצמיתות. **סעיף/תקנה:** סעיף 14(א); סיכון **15א**. **תיקון:** זרימת DSAR: התממת שדות PII בהזמנות (שמירת רשומה חשבונאית מנותקת מזהות) + מחיקת `CustomerProfile`/`User` ב-Auth; לתעד מתח soft-delete תפעולי מול hard-delete לפרטיות.
- 🟡 אין UI/API לתיקון פרטי לקוח (פרופיל) לאחר רישום — רק יצירה ב-`registerCustomer`. **סעיף/תקנה:** סעיף 14(א). **תיקון:** מסך "הפרופיל שלי" או תהליך תיקון ידני מתועד באדמין.
- 🟡 מחיקה עתידית חייבת לכסות נגזרות: Resend, גיבויי Supabase, לוגים — לא מטופל בקוד. **סעיף/תקנה:** סעיף 14. **תיקון:** רשימת מעבדים + נוהל מחיקה אצל צד-ג'.

### ו. עיבוד באמצעות AI (הנחיית 2025; תקנה 15) — N/A (למידע אישי)

- N/A לעיבוד **מידע אישי** ב-AI: `src/lib/ai/gemini-translate.ts` שולח **שמות מוצרים** (קטלוג) בלבד, מאחורי `requireAdmin`, ולא PII של לקוחות.
- 🔵 `src/lib/ai/gemini-translate.ts:41-55` — אם יופעל Gemini: ספק חיצוני (Google). לוודא DPA / תנאי שימוש ו-no-training ל-API key החינמי לפני שימוש בפרודקשן. **תקנה:** 15 (מיקור חוץ) — קונטקסט ארגוני.
- מדיניות הפרטיות אינה מזכירה AI — סביר כל עוד לא מעובד מידע אישי; אם יורחב לתוכן עם PII — חובה יידוע לפי הנחיית 2025.

---

## 4. תוכנית פעולה מתועדפת

| עדיפות | קטגוריה | התיקון הנדרש | עוגן בחוק | מאמץ |
|---|---|---|---|---|
| גבוהה | זכויות נושא-מידע | תהליך+כלי עיון/ייצוא מרוכז לכל PII של אדם | סעיף 13; 15א | בינוני |
| גבוהה | זכויות נושא-מידע | hard-delete / anonymize ל-PII בהזמנות+פרופיל (+Auth) | סעיף 14; 15א | בינוני–גבוה |
| גבוהה | בקרת גישה | MFA חובה לכל admin | תקנה 9(ב)(1) | נמוך (הגדרת Supabase) |
| גבוהה | לוגים | audit-log אוטומטי (5 שדות) + retention ≥24 ח' | תקנה 10(א) | בינוני |
| בינונית | הסכמה/יידוע | מדיניות פרטיות סופית + שמירת רשומת הסכמה בהזמנה | סעיף 11 | בינוני |
| בינונית | הסכמה | יידוע+הסכמה במסך רישום | סעיף 11 | נמוך |
| בינונית | צמצום | מדיניות retention + בחינה שנתית | תקנה 2(ג); סעיף 8(ב) | בינוני |
| בינונית | הצפנה | HSTS / אכיפת HTTPS באפליקציה | תקנה 14(ב) | נמוך |
| נמוכה | תיקון | מסלול עדכון פרופיל לקוח | סעיף 14 | נמוך |
| נמוכה | AI/מיקור חוץ | DPA מול Supabase, Vercel, Resend, (Gemini) | תקנה 15 | ארגוני |

---

## ממצאים בוודאות נמוכה — דורשים בדיקה אנושית

- 🟡? סיווג כתובת משלוח כ"נתוני מיקום" (רגיש-מיוחד) — בקוד זו מחרוזת כתובת בלבד, לא GPS. (הופרך חלקית: לשון סעיף 3 מדגישה נתוני מיקום/תעבורה; כתובת רחוב לרוב אישית. הרמה נשארת בינונית ממילא.)
- 🟡? האם Supabase Auth בפרויקט זה כבר מוגדר עם MFA אופציונלי מחוץ לקוד — לא נבדק ב-dashboard. (לא אושש בקוד — לבדוק ב-Supabase Console.)
- 🟡? `RESEND_DEV_TO` בפרודקשן: אם נשאר מוגדר, מיילים עם PII מופנים לתיבה אחרת — סיכון תהליכי/קונפיג (`.env.example:22-25`). לא אושש ערך פרודקשן בפועל.

## פערי כיסוי והמשך

המסמך **אינו ממצה**. מה שלא כוסה:
- **לא נסרק / מחוץ-לקוד:** DPA חתום מול Supabase/Vercel/Resend/Google · הצפנה במנוחה והפרדת רשת בפרויקט הענן · גיבויים ו-retention אצל הספק · רישום מאגר ברשות אם נדרש · תהליך ארגוני למענה לבקשות נושא-מידע · Pen-test (רלוונטי יותר לרמה גבוהה)
- **נקודות עיוורות:** מדיניות cookies/אנליטיקס (לא זוהו סקריפטים ייעודיים בסריקה זו) · תוכן מדויק של תבניות מייל ללקוח מעבר ל-payload · הרצת זמן-אמת של MFA ב-Supabase
- **טענות לא מאומתות:** היקף מספרי לקוחות/מורשים בפועל (הוערך כ-MVP קטן)

## 5. מתודולוגיה

- מבקרים (read-only): privacy-auditor ×6 קטגוריות (סיווג, הסכמה/צמצום, גישה/לוגים, הצפנה, זכויות נושא-מידע, AI).
- בסיס: תיקון 13 + תקנות 2017 + הנחיית AI 2025 (`israel-privacy-compliance/references/`).
- מקורות קוד עיקריים: `prisma/schema.prisma`, `src/lib/actions/orders.ts`, `CheckoutForm.tsx`, `src/content/legal/privacy.ts`, `src/lib/auth.ts`, `src/lib/actions/auth.ts`, `src/lib/notifications/*`, `src/lib/ai/gemini-translate.ts`, `next.config.ts`, `DOCS/Maison-Malka-ERD.md`.
- כל 🔴 נבדק מול השורות בפועל לפני שנכלל. הקוד שנבדק לא שונה.
- מגבלות: דברים שמחוץ-לקוד (תשתית, תהליך ארגוני, DPA חתום, הפרדת-רשת פיזית) צוינו כקונטקסט.
