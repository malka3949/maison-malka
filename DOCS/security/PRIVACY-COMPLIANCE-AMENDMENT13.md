# דוח תאימות — תיקון 13 לחוק הגנת הפרטיות ותקנות אבטחת מידע 2017

**תאריך:** 2026-07-14  ·  **מערכת/פרויקט:** Maison Malka  ·  **נתיב שנבדק:** `C:\Users\user1\Desktop\Masion-malka`
**הופק ע"י:** israel-privacy-compliance (ביקורת אוטומטית, read-only — Amendment 13 light pass)
**סוג ריצה:** light pass לפני ייצור (Phase 4) — המלצות תיעוד / remediation documentary; הקוד לא שונה.

> ⚠️ **אזהרה:** מסמך זה הוא ניתוח תאימות **טכני**, ולא ייעוץ משפטי. עמידה מלאה בחוק דורשת גם תשתיות
> מאובטחות, תהליכים ארגוניים, וליווי משפטי. הממצאים מבוססים על סריקת קוד בלבד.

---

## 1. תקציר מנהלים

Maison Malka היא חנות ממתקים אונליין (Next.js + Prisma + Supabase + Resend + Vercel) שאוספת ושומרת מידע אישי של לקוחות — שם, טלפון, אימייל, כתובת משלוח והיסטוריית הזמנות — ב-`Order` (כולל אורחים) וב-`CustomerProfile` (משתמשים רשומים). אין אחסון כרטיסי אשראי.

בשל **היסטוריית רכישות / פעילות הזמנות וסכומים** (סיווג כמידע בעל רגישות מיוחדת לפי תיקון 13) המאגר מסווג לרמת אבטחה **בינונית**. הפער החמור ביותר: **אין מסלול עיון/תיקון/מחיקה ודאית לנושא המידע** (חשיפה לסעיף 15א), לצד היעדר MFA ו-audit-log לגישת מנהלים, והיעדר יידוע/הסכמה על צדדים שלישיים בטופס ההזמנה.

| חומרה | כמות |
|---|---|
| 🔴 קריטי | 3 |
| 🟡 סיכון | 5 |
| 🔵 קל | 2 |

**שלושת הפערים לטיפול מיידי:**
1. 🔴 זכויות נושא המידע — אין hard-delete / התממה / ייצוא מרוכז של PII ב-Order ו-CustomerProfile (סעיפים 13–14; סיכון 15א).
2. 🔴 בקרת גישה — אין MFA למורשי admin הצופים ב-PII (תקנה 9(ב)(1); מאגר בינוני).
3. 🔴 צמצום/הסכמה — איסוף PII ב-checkout בלי הודעת יידוע על מטרה וצדדים שלישיים (סעיף 11).

---

## 2. סיווג מידע ורמת אבטחה

**סוגי מידע שזוהו:**

| שדה / מקור | סיווג | היכן נשמר | רגיש-מיוחד? |
|---|---|---|---|
| `CustomerProfile.full_name` | אישי | PostgreSQL (Supabase) | לא |
| `CustomerProfile.phone` | אישי | PostgreSQL | לא |
| `CustomerProfile.email` | אישי | PostgreSQL (+ כפילות ב-`User.email`) | לא |
| `CustomerProfile.default_delivery_address` | אישי | PostgreSQL | לא (כתובת משלוח; לא GPS) |
| `Order.customer_name` | אישי | PostgreSQL (snapshot) | לא |
| `Order.customer_phone` | אישי | PostgreSQL | לא |
| `Order.customer_email` | אישי | PostgreSQL | לא |
| `Order.delivery_address` | אישי | PostgreSQL | לא |
| `Order.customer_notes` | אישי (טקסט חופשי) | PostgreSQL | לא (עלול לכלול עודף) |
| `Order.total` / `subtotal` / `payment_method` + היסטוריית הזמנות | אישי — **פעילות פיננסית / היסטוריית רכישות** | PostgreSQL | **כן** (תיקון 13 סעיף 3) |
| `User.email` + `role` | אישי (+ תפקיד) | PostgreSQL + Supabase Auth | לא |
| שם + אימייל בעסקאות Resend | אישי | מעבד צד-ג' (Resend) | לא |
| session cookies (Supabase Auth) | מזהה מקוון | דפדפן / Vercel | לא |

**רמת אבטחה נדרשת (תקנות 2017):** בינונית  
**נימוק:** שמירת **היסטוריית הזמנות/רכישות וסכומים** מסווגת כמידע בעל רגישות מיוחדת (פעילות פיננסית) לפי תיקון 13; לפי תקנות 2017 מאגר עם מידע רגיש-מיוחד → **בינונית** לפחות, גם אם מספר מורשים קטן (חנות יחידה / מעט מנהלים). אין כרטיסי אשראי — מפחית היקף PCI אך לא מבטל את סיווג היסטוריית הרכישות.

---

## 3. ניתוח פערים לפי קטגוריה

### א. סיווג מידע (סעיף 3) — ⚠️

- 🟡 `prisma/schema.prisma:165-184` — מודל `Order` מחזיק PII מלא + סכומים ו-`payment_method` ללא תיעוד סיווג/מטרה בקוד או במדיניות פרטיות. **סעיף/תקנה:** סעיף 3 (מידע אישי; פעילות פיננסית כרגיש-מיוחד). **תיקון (documentary):** להוסיף מדיניות סיווג מאגר + רשימת שדות למסמך הגדרות מאגר; לוודא שאיסוף כרטיס לא נכנס בטעות ל-MVP.
- 🔵 `prisma/schema.prisma:152-163` — `CustomerProfile` משכפל אימייל שכבר ב-`User`; סביר ל-prefill אך כפילות להישמר במודעות לזכות מחיקה. **סעיף/תקנה:** תקנה 2(ג). **תיקון:** בכל מחיקה/התממה — לטפל בשניהם.

### ב. צמצום מידע, מטרה והסכמה (סעיפים 8, 11; תק' 2(ג)) — ❌

- 🔴 `src/components/storefront/CheckoutForm.tsx:86-114` — טופס checkout אוסף שם/טלפון/אימייל/כתובת בלי הודעת יידוע (מטרה, חובה/רשות, למי יימסר — Supabase/Vercel/Resend). **סעיף/תקנה:** סעיף 11(1)–(3). **תיקון (documentary + UI):** הוסף קישור למדיניות פרטיות + טקסט יידוע קצר לפני שליחה; תעד מעבדים.
- 🟡 `src/lib/actions/orders.ts:41-172` — יצירת הזמנה שומרת PII ללא מדיניות retention / בחינה שנתית בקוד. **סעיף/תקנה:** תקנה 2(ג); סעיף 8(ב). **תיקון:** הגדר retention (למשל אנונימיזציה לאחר X חודשים בכפוף לצרכים חשבונאיים) ותעד בדיקה שנתית מחוץ לקוד.
- 🟡 `prisma/schema.prisma:176` — `customer_notes` טקסט חופשי — סיכון over-collection. **סעיף/תקנה:** תקנה 2(ג). **תיקון:** הגבל אורך; הנחיה ללקוח לא לכלול נתונים רגישים; אל תשלח הערות ל-Resend (כיום לא נשלחות — תקין).
- ✅ שדות הליבה (שם/טלפון/אימייל/כתובת למשלוח) נראים נחוצים למילוי הזמנה — צמצום בסיסי סביר למטרת מאגר ההזמנות.

### ג. בקרת גישה, הזדהות ולוגים (תקנות 8, 9, 10) — ❌

- 🔴 `src/lib/actions/auth.ts:8-35` — התחברות admin בסיסמה בלבד (`signInWithPassword`); אין MFA למורשים הצופים ב-PII. **תקנה:** 9(ב)(1) — זיהוי על אמצעי פיזי בשליטת המורשה. **תיקון (documentary):** הפעל MFA ב-Supabase Auth למנהלים / אכוף ברמת ארגון לפני פרודקשן עם PII אמיתי.
- 🔴 אין מנגנון audit-log אוטומטי לגישה להזמנות/PII (צפייה ברשימה/פירוט/שינוי סטטוס). גישה ב-`src/app/admin/(protected)/orders/[id]/page.tsx:27-70` ו-`src/lib/actions/admin-orders.ts:17-39` בלי תיעוד זהות/זמן/רכיב/סוג/אושר-נדחה. **תקנה:** 10(א) (מאגר בינוני). **תיקון:** טבלת `AuditLog` או לוג ייעודי עם 5 השדות + שמירה ≥ 24 חודשים; אל תכתוב PII מלא בלוג.
- ✅ `src/lib/auth.ts:13-27` + `src/app/admin/(protected)/layout.tsx:21-24` — `requireAdmin` אוכף תפקיד admin בצד שרת (תקנה 8 — הרשאה לפי תפקיד) לפני מסכי ניהול.
- 🟡 `src/middleware.ts:58-66` — middleware בודק רק קיום session ל-`/admin`, לא role; ההגנה האמיתית ב-layout/actions. **תקנה:** 8(א). **תיקון:** אופציונלי — חסימת non-admin גם ב-middleware כדי לצמצם חשיפת מסלולים.

### ד. הצפנה והפרדת רשתות (תקנות 12, 13, 14) — ⚠️

- ✅ `src/lib/notifications/resend.ts:25-36` — שליחה ל-Resend ב-`https://api.resend.com` (TLS במעבר). **תקנה:** 14(ב).
- 🟡 `prisma/schema.prisma:152-184` — PII (שם/טלפון/אימייל/כתובת) נשמר בטקסט גלוי באפליקציה; הצפנה-במנוחה תלויה ב-Supabase managed (מחוץ לקוד). **תקנה:** 12 / נוהג סביר לרמה בינונית. **תיקון (documentary):** ודא הצפנה at-rest בפרויקט Supabase Production; אל תייצא מאגרים להתקנים ניידים בלי הצפנה.
- 🟡 `next.config.ts:3-13` — אין הגדרת HSTS/security headers בקוד; ב-Vercel HTTPS בדרך כלל נאכף בתשתית. **תקנה:** 14. **תיקון:** תעד ב-ops / הוסף headers במידת הצורך (ראה גם infra-security-review).
- 🔵 (קונטקסט תשתית) הפרדת רשתות (תקנה 13(ב)) — דרישת רמה גבוהה בעיקר; כאן מאגר בינוני. מומלץ להפריד Preview/Prod ופרויקטי Supabase בעתיד.

### ה. זכויות נושא המידע — עיון/תיקון/מחיקה (סעיפים 13, 14; סיכון 15א) — ❌

- 🔴 אין API / מסלול אדמין / self-service ל**ייצוא מרוכז** של כל המידע על אדם (User + CustomerProfile + כל Order snapshots + נגזרות Resend). **סעיף/תקנה:** סעיף 13(א); חשיפה ל-15א. **תיקון (documentary):** הגדר נוהל + כלי שליפה לפי אימייל/טלפון; תעד מגבלות לגבי עותקים אצל Resend/גיבויים.
- 🔴 אין **מחיקה ודאית / התממה** של PII: אין `delete` על User/CustomerProfile/Order באפליקציה; גם מחיקת משתמש עתידית תשאיר snapshots ב-`Order` (`user_id` → SetNull ב-`prisma/schema.prisma:182`). Soft-delete תפעולי לא קיים — והמחיקה הנדרשת לדין (hard-delete/anonymize) גם לא קיימת. **סעיף/תקנה:** סעיף 14(א); סיכון 15א. **תיקון:** מימוש anonymize להזמנות (החלפת שם/טלפון/אימייל/כתובת בטוקנים) + מחיקת פרופיל/Auth; מדיניות גיבויים; מתח עם soft-delete תפעולי — לבקשת נושא מידע חובה מחיקה/התממה אמיתית.
- 🟡 אין מסלול תיקון לקוח ל-`CustomerProfile` מעבר לרישום מחדש / עדכון ב-upsert ברישום (`src/lib/actions/orders.ts:252-265`); אין עדכון פרופיל ייעודי; snapshots בהזמנות ישנות לא מתעדכנים. **סעיף/תקנה:** סעיף 14(א). **תיקון:** מסך עריכת פרופיל + נוהל תיקון ידני להזמנות כשנדרש.

### ו. עיבוד באמצעות AI (הנחיית 2025; תקנה 15) — N/A

N/A — לא זוהה עיבוד AI / LLM / מודל ML על מידע אישי בקוד האפליקציה.

### ז. מעבדים צד-ג' (תקנה 15 — מיקור חוץ; סעיף 11(3)) — ⚠️

| מעבד | תפקיד | PII שנמסר | ממצא |
|---|---|---|---|
| **Supabase** | DB + Auth (+ Storage לתמונות מוצר) | כל שדות Order/CustomerProfile/User | מעבד מאגר — נדרש DPA / הסכם עיבוד (מחוץ לקוד) |
| **Vercel** | אירוח אפליקציה / לוגים | עשוי לכלול מזהים בלוגים/בקשות | מעבד תשתית — DPA + מדיניות לוגים (אל תדפיסו PII מלא) |
| **Resend** | מיילים תפעוליים | `customer_email`, `customer_name`, מזהה הזמנה, סכום | `src/lib/notifications/resend.ts:67-70` + templates |

- 🟡 `src/lib/notifications/resend.ts:11-53` — העברת PII ל-Resend בלי יידוע משתמש ב-UI ובלי אזכור DPA בקוד/Docs ציבורי. **סעיף/תקנה:** סעיף 11(3); תקנה 15 (מיקור חוץ). **תיקון (documentary):** חתימת DPA עם Supabase/Vercel/Resend; עדכון מדיניות פרטיות; וידוא שמיילים רק תפעוליים (כיום — כן).
- ✅ לא נמצא חשיפת `RESEND_API_KEY` / `SUPABASE_SERVICE_ROLE_KEY` לקליינט בסריקה זו (מפתחות ב-env שרת).

---

## 4. תוכנית פעולה מתועדפת

| עדיפות | קטגוריה | התיקון הנדרש | עוגן בחוק | מאמץ |
|---|---|---|---|---|
| גבוהה | זכויות נושא המידע | נוהל + כלי ייצוא / hard-delete או anonymize ל-Order+Profile+Auth | סעיפים 13–14; 15א | בינוני–גבוה |
| גבוהה | הזדהות | MFA למנהלים (Supabase) | תקנה 9(ב)(1) | נמוך–בינוני |
| גבוהה | יידוע/הסכמה | מדיניות פרטיות + יידוע ב-checkout על מעבדים | סעיף 11 | נמוך |
| גבוהה | לוגים | audit-log לגישת admin ל-PII (5 שדות, 24 ח') | תקנה 10(א) | בינוני |
| בינונית | מיקור חוץ | DPA מול Supabase, Vercel, Resend | תקנה 15; סעיף 11(3) | נמוך (משפטי/ארגוני) |
| בינונית | צמצום | מדיניות retention + בחינה שנתית | תקנה 2(ג) | נמוך–בינוני |
| בינונית | הצפנה | אימות at-rest ב-Supabase + HSTS/headers כנדרש | תקנות 12, 14 | נמוך (תשתית) |
| נמוכה | גישה | חיזוק בדיקת role ב-middleware | תקנה 8(א) | נמוך |
| נמוכה | צמצום | הגבלת `customer_notes` | תקנה 2(ג) | נמוך |

---

## ממצאים בוודאות נמוכה — דורשים בדיקה אנושית

- 🟡? האם נתוני משלוח טקסטואליים ייחשבו גם כ"נתוני מיקום" רגישים-מיוחדים מעבר לסיווג כתובת — הפרשנות המשפטית אינה חד-משמעית מהקוד; הדוח הסתמך על **היסטוריית רכישות/סכומים** כבסיס לרמה בינונית. (הופרך חלקית: אין GPS בקוד.)
- 🟡? האם בפועל מופעלת הצפנת דיסק/ backups ב-Supabase Production — לא ניתן לאמת מקוד. ❔ לא אושש — לבדוק ב-dashboard.
- 🟡? היקף מורשים ומספר נושאי מידע בעתיד (>10 מורשים / >100k) — עשוי להעלות רמה; כיום מוערך מאגר קטן עם סיווג בינוני בגלל סוג המידע.

## פערי כיסוי והמשך

המסמך **אינו ממצה**. מה שלא כוסה / מחוץ-לקוד:
- **לא נסרק / מחוץ-לקוד:** DPA חתום · הגדרות מאגר פורמליות · MFA בפועל ב-Supabase · גיבויים ושחזור · הפרדת סביבות · התנהגות runtime ב-Vercel logs · רישום מאגר אצל הרשות (אם חל)
- **נקודות עיוורות:** תוכן מדויק של מדיניות הפרטיות (לא קיימת ב-repo) · הסכמי Resend/Supabase · retention של מיילים אצל Resend · האם Preview משתף אותו DB עם Prod
- **טענות לא מאומתות:** רמת הצפנה at-rest של ספק הענן

**Residuals ל-Phase 4 (light pass):** זכויות נושא המידע, MFA, audit-log, יידוע/מדיניות פרטיות, ו-DPA — מומלצים כהמלצות תיעוד/תכנון לפני או במקביל להשקת Production עם PII אמיתי; דוח זה אינו מאשר עמידה משפטית מלאה.

## 5. מתודולוגיה

- מבקרים (read-only): privacy-auditor ×1 (light pass — סיווג, צמצום/הסכמה, גישה/לוגים, הצפנה, זכויות נושא מידע, AI, צד-ג').
- בסיס: תיקון 13 + תקנות 2017 + הנחיית AI 2025 (`.cursor/skills/israel-privacy-compliance/references/`).
- מיפוי PII מ-`prisma/schema.prisma` (Order, CustomerProfile), זרימות checkout/admin/Resend, ו-ERD.
- כל 🔴 נבדק מול השורות בפועל לפני שנכלל. הקוד שנבדק **לא** שונה; נכתב רק קובץ דוח זה.
- מגבלות: תשתית, תהליך ארגוני, ו-DPA חתום צוינו כקונטקסט/המלצה documentary.
