# ממצאי הקשחת שרת — <HOST> — <YYYY-MM-DD>

> ביקורת read-only של מצב שרת חי דרך SSH (גישה · חומת אש · קצה · סודות · עדכונים · גיבויים · לוגים/ניטור ·
> מוכנות לאירוע). לא בוצע שום שינוי בשרת — רק פקודות קריאה הורצו. יעד: <TARGET>.
> זו ביקורת *מצב חי*; כיסוי תלוי בהרשאות ה-SSH ובכלים שקיימים על השרת.

## סיכום מנהלים

| חומרה | כמות |
|---|---|
| 🔴 קריטי | N |
| 🟡 סיכון | N |
| 🔵 מינורי | N |

3 הדברים לתקן ראשונים:
1. 🔴 `<command → output>` — <שורה אחת>
2. 🔴 …
3. 🟡 …

## מצב לפי קטגוריה

| קטגוריה | מצב |
|---|---|
| גישה ו-SSH (access) | FAIL / WARN / PASS / לא נבדק |
| חומת אש ופורטים (network) | … |
| חשיפה לאינטרנט (edge) | … |
| סודות / git / הרשאות (secrets) | … |
| עדכונים (updates) | … |
| גיבויים (backups) | … |
| לוגים וניטור (observability) | … |
| מוכנות לאירוע (incident) | … |

---

## גישה ו-SSH — FAIL | WARN | PASS
- 🔴 `sudo sshd -T → "passwordauthentication yes"` — אימות סיסמה פעיל ב-SSH, חשוף ל-brute-force.
  **חוק:** `02-access-ssh.md §1`. **תיקון:** `PasswordAuthentication no` + מפתחות בלבד.

## חומת אש ופורטים — …
## חשיפה לאינטרנט — …
## סודות / git / הרשאות — …
## עדכונים — …
## גיבויים — …
## לוגים וניטור — …
## מוכנות לאירוע — …

---

## ממצאים בוודאות נמוכה / לבדיקת אדם
ממצאים שלב האימות לא הצליח לאשר — מוצגים, לא נמחקים:
- 🟡? `<command>` — <טענה>. (<למה לא ודאי>)

## פערי כיסוי והמשך
ביקורת זו בודקת **מצב שרת חי בלבד**. לא נכלל כאן:
- **קונפיג ב-repo** (compose/nginx/Dockerfile על הדיסק) → `/infra-audit`.
- **קוד האפליקציה** → `/secure-audit`. **פרטיות / תיקון 13** → `/privacy-audit`.
- **נגישות HTTP חיה** (האם פורט/header באמת מגיב עכשיו) → `runtime-confirm`.
- **חומת אש של ספק הענן / security groups / רשת פיזית** → לא נראה מתוך השרת, מחוץ לטווח.
- פריטים שדרשו `sudo` ולא היתה הרשאה / כלי שלא קיים: <מה לא נבדק>.

## מתודולוגיה
- סוכן (read-only): server-hardening-auditor ×N, אחד לכל קטגוריה. בסיס: `server-hardening-review/references/`.
- כל ממצא מעוגן ל**פקודה + הפלט שלה**. לא בוצעה אף פקודה משנה (mutating).

<!-- FORMAT RULES:
- כל ממצא: `- <emoji> \`command → output\` — <בעיה: סימפטום + סיבת-שורש + השפעה>. **חוק:** <reference §section>. **תיקון:** <קצר>.`
- עיגון ל**פקודה + פלט** (לא קובץ, לא חוק). חומרה: 🔴 חשיפה לאינטרנט/סוד דלוף/root-login/תעודה פגה ·
  🟡 הקשחה חסרה · 🔵 מינורי. לא בטוח → סעיף "ממצאים בוודאות נמוכה" כ-`❔ לא אומת — לבדוק שוב`, לא למחוק.
- אין תיקון ברור? `**תיקון:** לא נמצא תיקון — דרושה החלטת אדם (סיבה: …)`. לעולם לא להמציא ביטחון או תיקון.
- שפה: עברית, מונחים טכניים באנגלית. -->
